import { LoadingState } from '@/components/ui/LoadingSpinner';

export default function Loading() {
  return <LoadingState label="Loading DICE Ministry..." className="min-h-screen rounded-none border-0" />;
}
