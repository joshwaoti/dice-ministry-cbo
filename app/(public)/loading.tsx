import { LoadingState } from '@/components/ui/LoadingSpinner';

export default function PublicLoading() {
  return <LoadingState label="Loading page..." className="min-h-screen rounded-none border-0" />;
}
